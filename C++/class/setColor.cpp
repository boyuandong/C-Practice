#include<iostream>
using namespace std;
enum colour {red, yellow, blue, white, black};
class setColor
{
    private:
        int *set;   // a pointer to a array
        int n;      // length of array
        int e;      // number of elements in the set
    public:

};